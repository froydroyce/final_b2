<h1><%= @student.name %></h1>

<h2><u>Enrolled Courses</u></h2>

<% @student.courses.each do |course| %>
  <section id="course-<%= course.id %>">
    <ul>
      <li><%= course.name %>, Grade: <%= course.grade_for(@student) %></li>
    </ul>
  </section>
<% end %>
